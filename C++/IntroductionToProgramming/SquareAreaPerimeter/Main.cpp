#include<iostream>
using namespace std;

int main()
{
	int a, P, S;

	cout << "a = ";
	cin >> a;
	
	P = 4 * a;
	S = a * a;

	cout << "A square with side a = " << a << " has Perimeter P = " << P << " and Area S = " << S << endl;

	return 0;
}